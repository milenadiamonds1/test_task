# Getting started:

add .env file in Client folder with data: REACT_APP_BASE_URL=http://localhost:5001/

# Install dependencies
npm install server
npm install client

# Start both servers
npm start server
npm start client
